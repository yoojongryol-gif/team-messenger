rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 채팅 첨부파일: 로그인한 사람만 업로드/조회, 25MB 이하
    match /chat/{roomId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.resource.size < 25 * 1024 * 1024;
    }
  }
}
